apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agentteams-java.fullname" . }}-control-plane
data:
{{- range $key, $value := .Values.controlPlane.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
