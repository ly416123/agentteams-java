package io.agentteams.application.api;

/** Connectivity boundary used to reach an MCP service. */
public enum McpConnectivityMode {
    PLATFORM_PUBLIC,
    CUSTOMER_CONNECTOR,
    PRIVATE_DEPLOYMENT
}
