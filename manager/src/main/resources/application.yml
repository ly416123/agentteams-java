spring:
  application:
    name: agentteams-manager
  flyway:
    enabled: true
    locations: classpath:db/manager-migration
management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
  endpoint:
    health:
      probes:
        enabled: true
