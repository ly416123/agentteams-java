spring:
  application:
    name: agentteams-manager
  flyway:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
  endpoint:
    health:
      probes:
        enabled: true
